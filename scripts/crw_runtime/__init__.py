"""Runtime installation and diagnosis for the bridge and the relay.

Standard library only, and importable on the Python this repository runs its own checks
with. The components it installs need a newer interpreter than this package does; see
definition.requires_python and the install command for how that one is chosen.
"""
