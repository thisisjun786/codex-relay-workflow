interface:
  display_name: "Linear Check"
  short_description: "Linear 완료 기준을 검증하고 기존 작업에 수정 전달"
  default_prompt: "$linear-check 합의한 기획과 구현을 대조하고, 진행 중인 위임 작업의 범위 안에서 필요한 수정은 해당 작업에 바로 전달한 뒤 결과를 재검증해줘."
policy:
  allow_implicit_invocation: true
